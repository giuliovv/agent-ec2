# Open Questions

1. Do we bill fixed tiers (e.g., 30m GPU) or metered usage?
2. Should users prepay wallet credit to reduce per-run friction?
3. Do we support custom AMIs or only curated images?
4. Do agents upload code tarballs or pull from Git repos?
5. What isolation level is required (VM-only vs nested container sandbox)?
6. Do we support spot instances in MVP or only on-demand?
